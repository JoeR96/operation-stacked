export const environment = {
  production: true,
  baseUrlAuth: 'https://app.operationstacked.com/auth/', // Production base URL for auth app
  baseUrlWorkout: 'https://app.operationstacked.com/workout/', // Production base URL for workout app
  databaseConnectionString: 'your_production_db_connection_string',
};
